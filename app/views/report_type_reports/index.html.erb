<h1>Listing report_type_reports</h1>

<table>
  <tr>
    <th>Report</th>
    <th>Report type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @report_type_reports.each do |report_type_report| %>
  <tr>
    <td><%= report_type_report.report_id %></td>
    <td><%= report_type_report.report_type_id %></td>
    <td><%= link_to 'Show', report_type_report %></td>
    <td><%= link_to 'Edit', edit_report_type_report_path(report_type_report) %></td>
    <td><%= link_to 'Destroy', report_type_report, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Report type report', new_report_type_report_path %>
